@using JuliePro_Models.ViewModels
@model GenericControllerUpsertVM<Category>
@{
ViewData["Title"] = SharedLocals[Model.GeneralViewInfos.PageTitle];
}

<div class="row justify-content-center view-upsert">
    <div class="col-8">
        @{
            ViewData["Id"] = Model.Id;
        }
        <partial name="_ViewHeader" model="Model.GeneralViewInfos" view-data="ViewData" />
        <form asp-action="Upsert">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IsCreate" />
            <input type="hidden" asp-for="Entity.Id" />
            <div class="form-floating mb-3">
                <input asp-for="Entity.CategoryName" class="form-control" id="CategoryName" placeholder="@Html.DisplayNameFor(model => model.Entity.CategoryName)" />
                <label for="CategoryName" asp-for="Entity.CategoryName"></label>
                <span asp-validation-for="Entity.CategoryName" class="text-danger"></span>
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-primary">@SharedLocals[Model.GeneralViewInfos.SubmitButtonText]</button>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
